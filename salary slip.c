#include <stdio.h>
float main()
{
    float bs, ta, da, hra, gpf, net;
    printf("Enter your basic salary : ";)
    scanf("%d", &bs);

    if(bs<=25000)
    {
        ta = (bs*5)/100;
        da = (bs*50)/100;
        hra = (bs*7)/100;
        gpf = (bs*10)/100;
    }
    else if
}